terminfo for eterm-color
========================

To install just clone to the remote machine & run install.sh

        git clone git@github.com:ultrahertz/emacs-terminfo.git
        cd emacs-terminfo
        ./install.sh

